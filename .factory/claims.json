[
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit",
    "where": "landing page and README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context on /demo; wait for the service worker, go offline, and reload"
  },
  {
    "id": "local-only",
    "claim": "Workout data stays in this browser, with no analytics or third-party runtime requests",
    "where": "landing page, privacy page, and README",
    "test": "npm test -- --grep @claim:local-only",
    "sandbox": "fresh browser context on /demo; save a workout and assert every request is same-origin"
  },
  {
    "id": "progression-rule",
    "claim": "Rep results and limiting chips control hold, add reps, or increase",
    "where": "landing rule board, workout log, and README",
    "test": "npm test -- --grep @claim:progression-rule",
    "sandbox": "demo bench session; enter top reps, add Grip slipped, and assert the result changes to hold"
  },
  {
    "id": "csv-export",
    "claim": "Exports every saved set as CSV",
    "where": "backup page and README",
    "test": "npm test -- --grep @claim:csv-export",
    "sandbox": "seeded demo; download CSV and compare its row count with saved set rows"
  },
  {
    "id": "encrypted-backup",
    "claim": "Creates and imports a password-encrypted backup without storing its password",
    "where": "backup page, privacy page, and README",
    "test": "npm test -- --grep @claim:encrypted-backup",
    "sandbox": "seeded demo; download an encrypted file, assert its format and hidden workout text, then import it"
  },
  {
    "id": "paid-unlimited",
    "claim": "A $19 one-time license adds unlimited exercise templates",
    "where": "landing pricing, backup page, and README",
    "test": "npm test -- --grep @claim:paid-unlimited",
    "sandbox": "seeded demo; assert the free limit, mock a valid Sociobot verification response, then add a fourth exercise"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo changes and licenses never enter the real log and are discarded when the visitor starts for real",
    "where": "demo banner, demo documentation, and README",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser context; change demo sessions, restore a mocked demo license, leave demo, and inspect both real and reseeded demo storage"
  },
  {
    "id": "license-restore",
    "claim": "A license restores access when pasted into a fresh browser store",
    "where": "landing pricing and backup page",
    "test": "npm test -- --grep @claim:license-restore",
    "sandbox": "fresh browser context with an empty localStorage; mock the Sociobot valid response, paste a token, and reload"
  },
  {
    "id": "checkout-redirect",
    "claim": "The $19 buy action opens the hosted Dodo Live checkout",
    "where": "landing pricing and backup page",
    "test": "npm test -- --grep @claim:checkout-redirect",
    "sandbox": "load the local landing page, assert the exact Sociobot endpoint, request it without following redirects, and require a 303 to checkout.dodopayments.com/session/"
  }
]
